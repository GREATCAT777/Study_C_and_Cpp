/*#include<stdio.h>
#include<stdlib.h>

int main() {

	//삼항 연산자

	int num1;
	
	printf("원하는 숫자를 입력해주세요" );

	scanf_s("%d", &num1);

	printf("%d\n", num1 > 2 ? 3 : 0);

	printf("%s\n", num1 > 2 ? "2을 넘어가여 3으로 강제 치환 하였습니다" : "2을 넘어가지 못하여 0으로 강제 치환 하였습니다");

	//num1 > 2.0f           조건문제식
	//?                     판별식
	//3.0f : 4.0f           참일때 앞 출력 거짓일때 뒤출력
	//printf로 선언해줄때 %d,%f,%s등등 주의해주기
	return 0;
}*/